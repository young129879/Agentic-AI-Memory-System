# HMLR v1 - Core Dependencies
# Python 3.10+ required

# LLM Providers
openai>=1.0.0
anthropic>=0.75.0

# Vector Embeddings & Search
sentence-transformers>=2.2.0
torch>=2.0.0
transformers>=4.30.0

# Database & Storage  
# sqlite3 is built-in to Python (no install needed)

# Async Support
# asyncio is built-in to Python (no install needed)
aiohttp>=3.8.0
httpx>=0.24.0  # Async HTTP client for API calls

# Memory Service (hmlr.proxy)
# Only needed to run HMLR as an HTTP service; the library works without them.
fastapi>=0.110.0
uvicorn[standard]>=0.27.0

# RAGAS Testing (Optional - for validation)
ragas>=0.4.0
langsmith>=0.2.0  # Optional - for test result tracking
datasets>=2.14.0  # Required by RAGAS


# Utilities
python-dotenv>=1.0.0
pydantic>=2.0.0
tiktoken>=0.5.0  # Token counting for OpenAI models

